<Page x:Class="ocean.UI.GeneralDebugPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="clr-namespace:ocean.ViewModels"
      xmlns:mvvm="clr-namespace:ocean.Mvvm"
      xmlns:interactivity="http://schemas.microsoft.com/xaml/behaviors"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="GeneralDebugPage" Unloaded="Page_Unloaded" Loaded="Page_Loaded">

    
    
    <Grid>
        <!-- 添加资源：布尔值转可见性的转换器 -->
        <Grid.Resources>
            <BooleanToVisibilityConverter x:Key="BoolToVisConverter"/>
            <local:BindingProxy x:Key="ModbusSetProxy" Data="{Binding ModbusSet}"/>
            <mvvm:BoolToButtonTextConverter x:Key="BoolToButtonTextConverter"/>
        </Grid.Resources>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="26*"/>
            <ColumnDefinition Width="100*"/>
        </Grid.ColumnDefinitions>
        <Grid Grid.RowSpan="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="122"  MinWidth="122" MaxWidth="200"/>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="90*"/>
                <ColumnDefinition Width="2*"/>
            </Grid.ColumnDefinitions>
        </Grid>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="26*"/>
                <ColumnDefinition Width="40*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="20*"/>
                <RowDefinition Height="20*"/>
                <RowDefinition Height="20*"/>
                <RowDefinition Height="20*"/>
                <RowDefinition Height="20*"/>
                <RowDefinition Height="20*"/>
                <RowDefinition Height="100*"/>
                <RowDefinition Height="20*"/>
                <RowDefinition Height="20*"/>
                <RowDefinition Height="20*"/>
            </Grid.RowDefinitions>
            <TextBlock 
                      HorizontalAlignment="Left" 
                      Grid.Row="0"
                      Text="协议类型"
                      FontSize="10" 
                      Grid.ColumnSpan="2"  
                      Margin="10,0,0,0"
                      TextAlignment="Center"
                      VerticalAlignment="Center"/>
            <ComboBox
                Name="cPro" 
                FontSize="10" 
                Grid.Row="0" 
                Margin="60,1,0,1" 
                Grid.ColumnSpan="2"
                ItemsSource="{Binding ModbusSet.ProOptions}"
                SelectedItem="{Binding ModbusSet.ProSelectedOption, Mode=TwoWay}" SelectionChanged="cPro_SelectionChanged"/>

            <Button  Content="添加条目"  Grid.Row="1" Grid.ColumnSpan="2" Click="ButtonAdd_Click"  />
            <Button  Content="删除条目" Grid.Row="2" Grid.ColumnSpan="2" Click="ButtonDelete_Click"></Button>
            <Button Content="导出配置" Grid.Row="3" Grid.ColumnSpan="2" Click="ExportConfig_Click"/>
            <Button Content="导入配置" Grid.Row="4" Grid.ColumnSpan="2" Click="ImportConfig_Click"/>
                        <!-- 新增的切换按钮 -->
            <Button 
                Grid.Row="5" 
                Grid.ColumnSpan="2"
                Content="{Binding ModbusSet.IsAdvancedColumnsVisible, 
                          Converter={StaticResource BoolToButtonTextConverter}}"
                Command="{Binding ModbusSet.ToggleAdvancedColumnsCommand}"/>
            <Grid Name="Setadd" Grid.Row="6" Grid.ColumnSpan="2" Visibility="Hidden">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="20*"/>
                    <ColumnDefinition Width="40*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="10*"/>
                    <RowDefinition Height="10*"/>
                    <RowDefinition Height="10*"/>
                    <RowDefinition Height="10*"/>
                    <RowDefinition Height="20*"/>
                    <RowDefinition Height="10*"/>
                </Grid.RowDefinitions>
                <TextBox Text="参数名" 
                         Grid.Row="0" />
                <TextBox Text="数值" Grid.Row="0" 
                         Grid.Column="1" />
                <TextBox Text="数据条数" Grid.Row="1"></TextBox>
                <TextBox Text="{Binding ModbusSet.Snum, Mode=TwoWay}" 
                         Grid.Row="1" Grid.Column="1"></TextBox>
                <TextBox Text="类型" Grid.Row="2"></TextBox>
                <ComboBox Name="cbProcho" 
                          FontSize="10" 
                          Grid.Row="2" 
                          Grid.Column="1"  
                          Margin="0,1,0,1" 
                          Grid.ColumnSpan="2" 
                          ItemsSource="{Binding ModbusSet.Options}"
                          SelectedItem="{Binding ModbusSet.DSelectedOption, Mode=TwoWay}"/>
                <TextBox Text="起始地址"
                         Grid.Row="3"/>
                <TextBox Text="{Binding ModbusSet.Sadd, Mode=TwoWay}"
                         Grid.Row="3" 
                         Grid.Column="1"/>
                <Button Content="确定" Grid.Row="5" Grid.Column="0" 
                        Click="setsure_click" ></Button>
                <Button Content="取消" Grid.Row="5" Grid.Column="1" 
                        Click="setcancel_click" ></Button>
            </Grid>



        </Grid>

        <Grid  Grid.Column="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="100*"/>
                <RowDefinition Height="1*"/>
                <RowDefinition Height="20*"/>
            </Grid.RowDefinitions>
            <TabControl>
                <DataGrid x:Name="dataGrodx"
                                ItemsSource="{Binding ModbusSet.dtm, Mode=TwoWay}"
                                AutoGenerateColumns="False"
                                CanUserAddRows="False">
                        <!-- 使用Interactivity行为绑定命令（替代传统事件） -->
                    <interactivity:Interaction.Triggers>
                        <interactivity:EventTrigger EventName="MouseDoubleClick">
                            <interactivity:InvokeCommandAction 
                            Command="{Binding ModbusSet.DataGridDoubleClickCommand}"
                            CommandParameter="{Binding ElementName=dataGrodx}"/>
                            <!-- 直接传DataGrid控件 -->
                        </interactivity:EventTrigger>
                    </interactivity:Interaction.Triggers>
                    <DataGrid.Columns>
                            <!-- 前四列：普通文本 -->
                            <DataGridTextColumn Header="序号" Binding="{Binding ID}"/>
                            <DataGridTextColumn Header="名称" Binding="{Binding Name}"/>
                            <DataGridTemplateColumn Header="数值" IsReadOnly="True">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <!-- 给TextBlock绑定双击事件 -->
                                        <TextBlock Text="{Binding Value}"
                                                   Background="LightSkyBlue"  />
                                        <!-- 鼠标悬浮显示手型，提升体验 -->
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTextColumn Header="指令" Binding="{Binding Command, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                            <!-- 第五列：按钮列 -->
                            <DataGridTemplateColumn Header="写" Width="100">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button Content="写" 
                                        Command="{Binding Source={x:Static local:AppViewModel.Instance}, Path=ModbusSet.ButtonCommand}"
                                        CommandParameter="{Binding}"/>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTextColumn Header="单位" Binding="{Binding Unit}"/>
                            <DataGridTextColumn Header="范围" Binding="{Binding Rangle}"/>
                            <!-- ：选择框列 -->
                            <DataGridTemplateColumn Header="区块" Width="120" >
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <ComboBox FontSize="10" 
                                                  SelectedItem="{Binding SelectedOption, Mode=TwoWay}"
                                                  ItemsSource="{Binding DataContext.ModbusSet.Options, 
                              RelativeSource={RelativeSource AncestorType=DataGrid}}" />
                                        <!-- 若选项是对象，需指定显示字段 -->
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTextColumn Header="地址" Binding="{Binding Addr}"/>
                            <DataGridTextColumn Header="数量" Binding="{Binding Number}"/>
                            <DataGridTextColumn Header="位偏移" Binding="{Binding NOffSet}"/>
                            <DataGridTextColumn Header="位数" Binding="{Binding NBit}"/>
                            <!-- 以下是需要隐藏/显示的高级列（默认隐藏） -->
                            <DataGridTextColumn Header="系数" 
                                                Binding="{Binding Coefficient}" 
                                                Visibility="{Binding Data.IsAdvancedColumnsVisible, 
                                                Source={StaticResource ModbusSetProxy}, 
                                                Converter={StaticResource BoolToVisConverter}}"/>
                            <DataGridTextColumn Header="偏移" 
                                                Binding="{Binding Offset}" 
                                                 Visibility="{Binding Data.IsAdvancedColumnsVisible, 
                                                 Source={StaticResource ModbusSetProxy}, 
                                                 Converter={StaticResource BoolToVisConverter}}"/>
                            <DataGridTextColumn Header="小数位数" 
                                                Binding="{Binding DecimalPlaces}" 
                                                Visibility="{Binding Data.IsAdvancedColumnsVisible, 
                                                Source={StaticResource ModbusSetProxy}, 
                                                Converter={StaticResource BoolToVisConverter}}"/>
                
                            <!-- ：选择框列 -->
                        <DataGridTemplateColumn Header="传输类型" Width="120" 
                                                Visibility="{Binding Data.IsAdvancedColumnsVisible, 
                                                Source={StaticResource ModbusSetProxy}, 
                                                Converter={StaticResource BoolToVisConverter}}" >
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <ComboBox FontSize="10" 
                                                      SelectedItem="{Binding TransferType, Mode=TwoWay}"
                                                      ItemsSource="{Binding DataContext.ModbusSet.TransferTypeOptions, 
                                                                    RelativeSource={RelativeSource AncestorType=DataGrid}}" />
                                    <!-- 若选项是对象，需指定显示字段 -->
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        
                        <!-- ：选择框列 -->
                        <DataGridTemplateColumn Header="呈现类型" Width="120" 
                                                Visibility="{Binding Data.IsAdvancedColumnsVisible, 
                                                Source={StaticResource ModbusSetProxy}, 
                                                Converter={StaticResource BoolToVisConverter}}" >
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <ComboBox FontSize="10" 
                                                      SelectedItem="{Binding DisplayType, Mode=TwoWay}"
                                                      ItemsSource="{Binding DataContext.ModbusSet.DisplayTypeOptions, 
                                                                    RelativeSource={RelativeSource AncestorType=DataGrid}}" />
                                    <!-- 若选项是对象，需指定显示字段 -->
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <DataGridTextColumn Header="字节序" 
                                                Binding="{Binding ByteOrder}" 
                                                 Visibility="{Binding Data.IsAdvancedColumnsVisible, 
                                                 Source={StaticResource ModbusSetProxy}, 
                                                 Converter={StaticResource BoolToVisConverter}}"/>
                            <DataGridTextColumn Header="字序" 
                                                Binding="{Binding WordOrder}" 
                                                 Visibility="{Binding Data.IsAdvancedColumnsVisible, 
                                                 Source={StaticResource ModbusSetProxy}, 
                                                 Converter={StaticResource BoolToVisConverter}}"/>
                            <DataGridCheckBoxColumn Header="是否绘制曲线" Binding="{Binding IsDrawCurve}" 
                                                     Visibility="{Binding Data.IsAdvancedColumnsVisible, 
                                                     Source={StaticResource ModbusSetProxy}, 
                                                     Converter={StaticResource BoolToVisConverter}}"/>
                            <DataGridTextColumn Header="间隔时间（ms）" 
                                                Binding="{Binding IntervalTime}" 
                                                Visibility="{Binding Data.IsAdvancedColumnsVisible, 
                                                Source={StaticResource ModbusSetProxy}, 
                                                Converter={StaticResource BoolToVisConverter}}"/>
                    </DataGrid.Columns>
                </DataGrid>
            </TabControl>
            <TextBox TextWrapping="Wrap" 
                     Name="show_text"  
                     Grid.Column="2"
                     Grid.Row="3" 
                     Margin="3,0,0,0" 
                     AcceptsReturn="True" 
                     Grid.RowSpan="2" 
                     Text="{Binding ModbusSet.BoxStr,Mode=TwoWay}"
                     ScrollViewer.VerticalScrollBarVisibility="Auto"
                     TextChanged="ShowText_TextChanged">
            </TextBox>
        </Grid>
       
    </Grid>
</Page>
