<Page x:Class="ocean.UI.Modserial"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:ocean.UI"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Modserial" Unloaded="Page_Unloaded">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="26*"/>
            <ColumnDefinition Width="100*"/>
        </Grid.ColumnDefinitions>
        <Grid Grid.RowSpan="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="122"  MinWidth="122" MaxWidth="200"/>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="90*"/>
                <ColumnDefinition Width="2*"/>
            </Grid.ColumnDefinitions>
        </Grid>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="26*"/>
                <ColumnDefinition Width="40*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="20*"/>
                <RowDefinition Height="20*"/>
                <RowDefinition Height="20*"/>
                <RowDefinition Height="20*"/>
                <RowDefinition Height="20*"/>
                <RowDefinition Height="20*"/>
                <RowDefinition Height="100*"/>
                <RowDefinition Height="20*"/>
                <RowDefinition Height="20*"/>
                <RowDefinition Height="20*"/>
            </Grid.RowDefinitions>
            <Button  Content="添加条目"  Grid.Row="1" Grid.ColumnSpan="2" Click="ButtonAdd_Click"  />
            <Button  Content="删除条目" Grid.Row="2" Grid.ColumnSpan="2" Click="ButtonDelete_Click"></Button>
            <Grid Name="Setadd" Grid.Row="6" Grid.ColumnSpan="2" Visibility="Hidden">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="20*"/>
                    <ColumnDefinition Width="40*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="10*"/>
                    <RowDefinition Height="10*"/>
                    <RowDefinition Height="10*"/>
                    <RowDefinition Height="10*"/>
                    <RowDefinition Height="20*"/>
                    <RowDefinition Height="10*"/>
                </Grid.RowDefinitions>
                <TextBox Text="参数名" Grid.Row="0"></TextBox>
                <TextBox Text="数值" Grid.Row="0" Grid.Column="1"></TextBox>
                <TextBox Text="数据条数" Grid.Row="1"></TextBox>
                <TextBox Text="{Binding mcom.snum, Mode=TwoWay}" Grid.Row="1" Grid.Column="1"></TextBox>
                <TextBox Text="类型" Grid.Row="2"></TextBox>
                <ComboBox Name="cbProcho" FontSize="10" Grid.Row="2" Grid.Column="1"  Margin="0,1,0,1" 
                          Grid.ColumnSpan="2" 
                          SelectionChanged="cbProcho_SelectionChanged" 
                          ItemsSource="{Binding Options}"
                          SelectedItem="{Binding mcom.dSelectedOption, Mode=TwoWay}"/>
                <TextBox Text="起始地址" Grid.Row="3"></TextBox>
                <TextBox Text="{Binding mcom.Sadd, Mode=TwoWay}" Grid.Row="3" Grid.Column="1"></TextBox>
                <Button Content="确定" Grid.Row="5" Grid.Column="0" Click="setsure_click" ></Button>
                <Button Content="取消" Grid.Row="5" Grid.Column="1" Click="setcancel_click" ></Button>
            </Grid>
        </Grid>

        <Grid  Grid.Column="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="100*"/>
                <RowDefinition Height="1*"/>
                <RowDefinition Height="20*"/>
            </Grid.RowDefinitions>
            <TabControl>
                <DataGrid x:Name="dataGrodx"
                                ItemsSource="{Binding mcom.dtm, Mode=TwoWay}"
                                AutoGenerateColumns="False"
                          CanUserAddRows="False" 
                          MouseDoubleClick="DataGrodx_MouseDoubleClick">
                    <DataGrid.Columns>
                        <!-- 前四列：普通文本 -->
                        <DataGridTextColumn Header="序号" Binding="{Binding ID}"/>
                        <DataGridTextColumn Header="名称" Binding="{Binding Name}"/>
                        <DataGridTemplateColumn Header="数值" IsReadOnly="True">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <!-- 给TextBlock绑定双击事件 -->
                                    <TextBlock Text="{Binding Value}"
                                               Background="LightSkyBlue"  />
                                    <!-- 鼠标悬浮显示手型，提升体验 -->
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTextColumn Header="指令" Binding="{Binding Command, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                        <!-- 第五列：按钮列 -->
                        <DataGridTemplateColumn Header="写" Width="100">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Content="写" 
                            Command="{Binding DataContext.ButtonCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                            CommandParameter="{Binding}"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTextColumn Header="单位" Binding="{Binding Unit}"/>
                        <DataGridTextColumn Header="范围" Binding="{Binding Rangle}"/>
                        <!-- ：选择框列 -->
                        <DataGridTemplateColumn Header="区块" Width="120">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <ComboBox FontSize="10" 
                                              SelectedItem="{Binding SelectedOption, Mode=TwoWay}"
                                              ItemsSource="{Binding ElementName=dataGrodx, Path=DataContext.Options}" />
                                    <!-- 若选项是对象，需指定显示字段 -->
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTextColumn Header="地址" Binding="{Binding Addr}"/>
                        <DataGridTextColumn Header="数量" Binding="{Binding Number}"/>
                        <DataGridTextColumn Header="偏移" Binding="{Binding NOffSet}"/>
                        <DataGridTextColumn Header="位数" Binding="{Binding NBit}"/>
                    </DataGrid.Columns>
                </DataGrid>
            </TabControl>
            <TextBox TextWrapping="Wrap" Name="show_text"  
          Grid.Column="2"
          Grid.Row="3" Margin="3,0,0,0" AcceptsReturn="True" Grid.RowSpan="2" Text="{Binding mcom.BoxStr,Mode=TwoWay}"></TextBox>
        </Grid>
       
    </Grid>
</Page>
