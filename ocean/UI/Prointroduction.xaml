<Page x:Class="ocean.UI.Prointroduction"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:ocean.UI" 
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Prointroduction">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="20*"/>
            <ColumnDefinition Width="100*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock 
                Grid.Row="0"
                Grid.ColumnSpan="2"
                Text="协议介绍"
                FontSize="30" 
                TextAlignment="Center"
                VerticalAlignment="Center"/>

        <TextBlock 
                HorizontalAlignment="Left" 
                Grid.Row="1"
                Text="协议类型"
                FontSize="10" 
                Margin="10,0,0,0"
                TextAlignment="Center"
                VerticalAlignment="Center"/>

        <ComboBox
                Name="cPro" 
                FontSize="10" 
                Grid.Row="1" 
                Margin="60,20,0,20" 
                ItemsSource="{Binding ModbusSet.ProOptions}"
                SelectedItem="{Binding ModbusSet.ProSelectedOption, Mode=TwoWay}"/>


        <TextBox 
            Name="ty2" 
            Grid.Column="1" 
            Grid.Row="1"
            Grid.RowSpan="4"
            Text="{Binding Uvm.ChangeTextBox2.Text,Mode=TwoWay}" 
            Margin="5"  
            FontSize="12" 
            />


    </Grid>
    
    
    
</Page>
